{
  "name": "mazesolver_visualizer",
  "version": "1.0.0",
  "description": "Visualizes path finding algorithm",
  "main": "index.js",
  "scripts": {
    "start": "tsc --watch",
    "build": "esbuild src/* --bundle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.19.3"
  }
}
